@using EcommerceDemo.Models
@model Producto
@{
    ViewData["Title"] = "Editar Producto";
    ViewData["AdminPage"] = "productos";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    var categorias = ViewBag.Categorias as List<Categoria> ?? new();
}

<div class="card shadow-sm border-0" style="max-width:800px;">
    <div class="card-body">
        <form asp-action="EditarProducto" method="post">
            @Html.AntiForgeryToken()
            <input asp-for="Id" type="hidden" />
            <input asp-for="FechaCreacion" type="hidden" />
            <div class="row g-3">
                <div class="col-md-8">
                    <label class="form-label fw-semibold small">Nombre *</label>
                    <input asp-for="Nombre" class="form-control" />
                    <span asp-validation-for="Nombre" class="text-danger small"></span>
                </div>
                <div class="col-md-4">
                    <label class="form-label fw-semibold small">SKU *</label>
                    <input asp-for="SKU" class="form-control" />
                    <span asp-validation-for="SKU" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold small">Categoría *</label>
                    <select asp-for="CategoriaId" class="form-select">
                        @foreach (var c in categorias)
                        {
                            <option value="@c.Id" selected="@(Model.CategoriaId == c.Id ? "selected" : null)">@c.Nombre</option>
                        }
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-semibold small">Precio *</label>
                    <div class="input-group">
                        <span class="input-group-text">$</span>
                        <input asp-for="Precio" class="form-control" type="number" step="0.01" min="0" />
                    </div>
                    <span asp-validation-for="Precio" class="text-danger small"></span>
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-semibold small">Stock</label>
                    <input asp-for="Stock" class="form-control" type="number" min="0" />
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold small">Marca</label>
                    <input asp-for="Marca" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold small">URL de Imagen</label>
                    <input asp-for="ImagenUrl" class="form-control" />
                </div>
                <div class="col-12">
                    <label class="form-label fw-semibold small">Descripción</label>
                    <textarea asp-for="Descripcion" class="form-control" rows="3"></textarea>
                </div>
                <div class="col-12">
                    <label class="form-label fw-semibold small">Compatibilidad con autos</label>
                    <input asp-for="CompatibilidadAutos" class="form-control" />
                </div>
                <div class="col-12 d-flex gap-4">
                    <div class="form-check">
                        <input asp-for="Destacado" class="form-check-input" type="checkbox" />
                        <label asp-for="Destacado" class="form-check-label">Destacado</label>
                    </div>
                    <div class="form-check">
                        <input asp-for="Activo" class="form-check-input" type="checkbox" />
                        <label asp-for="Activo" class="form-check-label">Activo</label>
                    </div>
                </div>
            </div>
            <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-warning fw-semibold"><i class="fas fa-save me-1"></i>Guardar Cambios</button>
                <a asp-action="Productos" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }
